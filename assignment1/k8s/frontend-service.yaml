apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: banking-app
spec:
  selector: { app: frontend }
  ports:
    - port: 3000
      targetPort: 3000
      nodePort: 30080
  type: NodePort
